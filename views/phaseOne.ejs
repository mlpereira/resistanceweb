<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Resistance Web</title>
    <link rel="stylesheet" href="/stylesheets/index.css" />
  </head>
  <body>
    <h1>You are <%= (player.isLoyal ? 'Loyal' : 'a Spy') %>! %></h1>
    <% if (!player.isLoyal) { %>
      <h2>The other spies are:</h2>
      <ul>
      <% for (var i=0; i<room.players.length; i++) { %>
        <% if (!room.players[i].isLoyal && room.players[i].id != player.id) { %>
          <li><%= room.players[i].name %></li>
        <% } %>
      <% } %>
      </ul>
    <% } %>

  </body>
</html>
